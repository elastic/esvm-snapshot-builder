## autogenerated by generate-config.js
version: '2'
services:
  master:
    tty: true
    container_name: master
    image: esvm-builder-gradle
    env_file:
      - .env.defaults
      - .env
    environment:
      BRANCH: master
    volumes:
      - './repos/master:/repo'
  5.x:
    tty: true
    container_name: 5.x
    image: esvm-builder-gradle
    env_file:
      - .env.defaults
      - .env
    environment:
      BRANCH: 5.x
    volumes:
      - './repos/5.x:/repo'
  '5.3':
    tty: true
    container_name: '5.3'
    image: esvm-builder-gradle
    env_file:
      - .env.defaults
      - .env
    environment:
      BRANCH: '5.3'
    volumes:
      - './repos/5.3:/repo'
  '5.2':
    tty: true
    container_name: '5.2'
    image: esvm-builder-gradle
    env_file:
      - .env.defaults
      - .env
    environment:
      BRANCH: '5.2'
    volumes:
      - './repos/5.2:/repo'
  '2.4':
    tty: true
    container_name: '2.4'
    image: esvm-builder-maven
    env_file:
      - .env.defaults
      - .env
    environment:
      BRANCH: '2.4'
    volumes:
      - './repos/2.4:/repo'

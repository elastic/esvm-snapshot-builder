## autogenerated by generate-config.js
version: '2'
services:
  master:
    tty: true
    container_name: master
    image: esvm-builder-gradle
    env_file:
      - .env.defaults
      - .env
    environment:
      BRANCH: master
  5.x:
    tty: true
    container_name: 5.x
    image: esvm-builder-gradle
    env_file:
      - .env.defaults
      - .env
    environment:
      BRANCH: 5.x
  '5.5':
    tty: true
    container_name: '5.5'
    image: esvm-builder-gradle
    env_file:
      - .env.defaults
      - .env
    environment:
      BRANCH: '5.5'
